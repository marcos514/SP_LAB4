import { PipeCambioPipe } from './pipe-cambio.pipe';

describe('PipeCambioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCambioPipe();
    expect(pipe).toBeTruthy();
  });
});
